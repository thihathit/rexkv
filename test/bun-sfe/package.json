{
  "name": "bun-sfe",
  "private": true,
  "type": "module",
  "dependencies": {
    "red-kv": "file:../../ts"
  },
  "scripts": {
    "build": "bun build --compile index.ts --outfile bun-sfe",
    "format": "oxfmt index.ts native.d.ts",
    "test": "bun run build && ./bun-sfe",
    "typecheck": "tsc"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "oxfmt": "^0.62.0",
    "typescript": "^7.0.2"
  }
}
